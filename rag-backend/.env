# RM Volley RAG Backend Configuration
# Copy this file to .env and adjust values as needed

# =============================================================================
# LLM PROVIDER CONFIGURATION
# =============================================================================
# Choose your LLM provider: "ollama" (local) or "groq" (cloud)
# - ollama: Free, runs locally, requires GPU for good performance
# - groq: Free tier available, fast, runs in cloud
LLM_PROVIDER=ollama

# -----------------------------------------------------------------------------
# OLLAMA Configuration (when LLM_PROVIDER=ollama)
# -----------------------------------------------------------------------------
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral:7b

# Alternative Ollama models:
# OLLAMA_MODEL=mistral:7b        # Better reasoning, slower
# OLLAMA_MODEL=phi3:mini         # Fastest, smallest
# OLLAMA_MODEL=llama3.1:8b       # Most capable
# OLLAMA_MODEL=llama3.2:3b       # Good balance

# -----------------------------------------------------------------------------
# GROQ Configuration (when LLM_PROVIDER=groq)
# -----------------------------------------------------------------------------
# Get your free API key at: https://console.groq.com/keys
# GROQ_API_KEY=gsk_your_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Alternative Groq models:
# GROQ_MODEL=llama-3.3-70b-versatile   # Best quality, recommended
# GROQ_MODEL=llama-3.1-8b-instant      # Faster, smaller
# GROQ_MODEL=mixtral-8x7b-32768        # Good for long context
# GROQ_MODEL=gemma2-9b-it              # Google's model

# Database Configuration
DB_PATH=./volleyball_db

# Data Source Configuration
DATA_DIR=../
MATCHES_FILE=Gare.xls
STANDINGS_FILE=classifica.json

# Embedding Model Configuration
# IMPORTANT: Changing model requires re-indexing! Run: python indexer.py
EMBEDDING_MODEL=intfloat/multilingual-e5-small

# Alternative embedding models (uncomment to use):
# EMBEDDING_MODEL=all-MiniLM-L6-v2                              # 384 dim, fast, English-focused
# EMBEDDING_MODEL=intfloat/multilingual-e5-small                # 384 dim, best for Italian/multilingual
# EMBEDDING_MODEL=intfloat/multilingual-e5-base                 # 768 dim, higher quality, slower
# EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2  # 384 dim, good multilingual
# EMBEDDING_MODEL=BAAI/bge-m3                                   # 1024 dim, top quality, heavy

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:5500

# Logging
LOG_LEVEL=info

# ChromaDB Configuration
ANONYMIZED_TELEMETRY=false
